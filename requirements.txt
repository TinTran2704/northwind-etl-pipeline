# ============================================================
# ETL Data Warehouse — Python Dependencies
# Install: pip install -r requirements.txt
# ============================================================

# ── Core data ────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
pyarrow>=15.0.0             # Parquet support (CRC index, staging)
duckdb==0.10.3              # Local staging + fast SQL

# ── Database (PostgreSQL) ────────────────────────────────────
psycopg2-binary==2.9.9
sqlalchemy==2.0.30

# ── HTTP / Extract ───────────────────────────────────────────
requests==2.32.3
tenacity==8.3.0             # Retry logic

# ── Configuration ────────────────────────────────────────────
pyyaml==6.0.1
python-dotenv==1.0.1

# ── Fuzzy matching (Conform phase) ───────────────────────────
jellyfish==1.0.3

# ── Date utilities ───────────────────────────────────────────
python-dateutil==2.9.0

# ── Logging ──────────────────────────────────────────────────
structlog==24.2.0

# ── Data validation ──────────────────────────────────────────
pydantic==2.7.4

# ── Testing ──────────────────────────────────────────────────
pytest==8.2.2
pytest-cov==5.0.0
responses==0.25.3           # Mock HTTP requests in tests
requests-mock==1.12.1

# ── Streaming (Phase 2 — Kafka) ──────────────────────────────
confluent-kafka==2.4.0
kafka-python==2.0.2

# ── Big Data Transform (Phase 2 — Spark) ─────────────────────
pyspark==3.5.1
pytest-spark==0.6.0

# ── Development ──────────────────────────────────────────────
ipython==8.25.0
jupyterlab==4.2.2           # Optional: notebook exploration